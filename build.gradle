plugins {
    id 'maven-publish'
}

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.github.johnvuko'
            artifactId = 'BrotherPrintLibrary'
            version = '4.12.0'
            artifact file('libs/BrotherPrintLibrary.aar')
        }
    }
}
